<a href="/movies">Movies</a>
<a href="/directors">Directors</a>
<a href="/actors">Actors</a>
<br>

<h1>Edit Director #<%= @director.id %></h1>
<br>
<form action="/directors/update/<%= @director.id %>">
    <div class="">
  <label for="name">Name</label>
  <input id="name" type = "text" name="the_name" placeholder="Name" value="<%= @director.name %>">
  </div>
  <div>
      <label for="bio">Bio</label>
      <input id="bio" type = "integer" name="the_bio" placeholder="Bio" value="<%= @director.bio %>">
    </div>
    <div>
        <label for="dob">DOB</label>
        <input id="dob" type = "integer" name="the_dob" placeholder="DOB" value="<%= @director.dob %>">
      </div>
    <div>
      <label for="image_url">Image URL</label>
      <input id="image_url" type="text" name="the_image_url" placeholder="Image url" value="<%= @director.image_url %>">
    </div>
    <div>
      <button>EDIT DIRECTOR</button>
    </div>
  </form>
